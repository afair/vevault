// Package fuse provides FUSE mount/unmount for encrypted vaults (v1.1).
package fuse

// Placeholder for FUSE filesystem that exposes an encrypted vault as a
// plaintext directory. Deferred to v1.1 per Design.md §9.3.